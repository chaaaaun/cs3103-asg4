[project]
name = "gamenetapi"
version = "0.1.0"
readme = "README.md"

requires-python = ">=3.13"
dependencies = [
    "matplotlib>=3.10.7",
]

[build-system]
requires = ["uv_build>=0.9.5,<0.10.0"]
build-backend = "uv_build"
